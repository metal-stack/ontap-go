// Code generated by go-swagger; DO NOT EDIT.

package n_v_me

// This file was generated by the swagger tool.
// Editing this file might prove futile when you re-run the generate command

import (
	"errors"
	"net/url"
	golangswaggerpaths "path"
	"strings"

	"github.com/go-openapi/swag"
)

// NvmeSubsystemMapGetURL generates an URL for the nvme subsystem map get operation
type NvmeSubsystemMapGetURL struct {
	NamespaceUUID string
	SubsystemUUID string

	Fields []string

	_basePath string
	// avoid unkeyed usage
	_ struct{}
}

// WithBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *NvmeSubsystemMapGetURL) WithBasePath(bp string) *NvmeSubsystemMapGetURL {
	o.SetBasePath(bp)
	return o
}

// SetBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *NvmeSubsystemMapGetURL) SetBasePath(bp string) {
	o._basePath = bp
}

// Build a url path and query string
func (o *NvmeSubsystemMapGetURL) Build() (*url.URL, error) {
	var _result url.URL

	var _path = "/protocols/nvme/subsystem-maps/{subsystem.uuid}/{namespace.uuid}"

	namespaceUUID := o.NamespaceUUID
	if namespaceUUID != "" {
		_path = strings.Replace(_path, "{namespace.uuid}", namespaceUUID, -1)
	} else {
		return nil, errors.New("namespaceUuid is required on NvmeSubsystemMapGetURL")
	}

	subsystemUUID := o.SubsystemUUID
	if subsystemUUID != "" {
		_path = strings.Replace(_path, "{subsystem.uuid}", subsystemUUID, -1)
	} else {
		return nil, errors.New("subsystemUuid is required on NvmeSubsystemMapGetURL")
	}

	_basePath := o._basePath
	if _basePath == "" {
		_basePath = "/api"
	}
	_result.Path = golangswaggerpaths.Join(_basePath, _path)

	qs := make(url.Values)

	var fieldsIR []string
	for _, fieldsI := range o.Fields {
		fieldsIS := fieldsI
		if fieldsIS != "" {
			fieldsIR = append(fieldsIR, fieldsIS)
		}
	}

	fields := swag.JoinByFormat(fieldsIR, "csv")

	if len(fields) > 0 {
		qsv := fields[0]
		if qsv != "" {
			qs.Set("fields", qsv)
		}
	}

	_result.RawQuery = qs.Encode()

	return &_result, nil
}

// Must is a helper function to panic when the url builder returns an error
func (o *NvmeSubsystemMapGetURL) Must(u *url.URL, err error) *url.URL {
	if err != nil {
		panic(err)
	}
	if u == nil {
		panic("url can't be nil")
	}
	return u
}

// String returns the string representation of the path with query string
func (o *NvmeSubsystemMapGetURL) String() string {
	return o.Must(o.Build()).String()
}

// BuildFull builds a full url with scheme, host, path and query string
func (o *NvmeSubsystemMapGetURL) BuildFull(scheme, host string) (*url.URL, error) {
	if scheme == "" {
		return nil, errors.New("scheme is required for a full url on NvmeSubsystemMapGetURL")
	}
	if host == "" {
		return nil, errors.New("host is required for a full url on NvmeSubsystemMapGetURL")
	}

	base, err := o.Build()
	if err != nil {
		return nil, err
	}

	base.Scheme = scheme
	base.Host = host
	return base, nil
}

// StringFull returns the string representation of a complete url
func (o *NvmeSubsystemMapGetURL) StringFull(scheme, host string) string {
	return o.Must(o.BuildFull(scheme, host)).String()
}
