// Code generated by go-swagger; DO NOT EDIT.

package n_a_s

// This file was generated by the swagger tool.
// Editing this file might prove futile when you re-run the generate command

import (
	"errors"
	"net/url"
	golangswaggerpaths "path"
	"strings"

	"github.com/go-openapi/swag"
)

// CifsShareACLGetURL generates an URL for the cifs share acl get operation
type CifsShareACLGetURL struct {
	Share       string
	SvmUUID     string
	Type        string
	UserOrGroup string

	Fields []string

	_basePath string
	// avoid unkeyed usage
	_ struct{}
}

// WithBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *CifsShareACLGetURL) WithBasePath(bp string) *CifsShareACLGetURL {
	o.SetBasePath(bp)
	return o
}

// SetBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *CifsShareACLGetURL) SetBasePath(bp string) {
	o._basePath = bp
}

// Build a url path and query string
func (o *CifsShareACLGetURL) Build() (*url.URL, error) {
	var _result url.URL

	var _path = "/protocols/cifs/shares/{svm.uuid}/{share}/acls/{user_or_group}/{type}"

	share := o.Share
	if share != "" {
		_path = strings.Replace(_path, "{share}", share, -1)
	} else {
		return nil, errors.New("share is required on CifsShareACLGetURL")
	}

	svmUUID := o.SvmUUID
	if svmUUID != "" {
		_path = strings.Replace(_path, "{svm.uuid}", svmUUID, -1)
	} else {
		return nil, errors.New("svmUuid is required on CifsShareACLGetURL")
	}

	typeVar := o.Type
	if typeVar != "" {
		_path = strings.Replace(_path, "{type}", typeVar, -1)
	} else {
		return nil, errors.New("type is required on CifsShareACLGetURL")
	}

	userOrGroup := o.UserOrGroup
	if userOrGroup != "" {
		_path = strings.Replace(_path, "{user_or_group}", userOrGroup, -1)
	} else {
		return nil, errors.New("userOrGroup is required on CifsShareACLGetURL")
	}

	_basePath := o._basePath
	if _basePath == "" {
		_basePath = "/api"
	}
	_result.Path = golangswaggerpaths.Join(_basePath, _path)

	qs := make(url.Values)

	var fieldsIR []string
	for _, fieldsI := range o.Fields {
		fieldsIS := fieldsI
		if fieldsIS != "" {
			fieldsIR = append(fieldsIR, fieldsIS)
		}
	}

	fields := swag.JoinByFormat(fieldsIR, "csv")

	if len(fields) > 0 {
		qsv := fields[0]
		if qsv != "" {
			qs.Set("fields", qsv)
		}
	}

	_result.RawQuery = qs.Encode()

	return &_result, nil
}

// Must is a helper function to panic when the url builder returns an error
func (o *CifsShareACLGetURL) Must(u *url.URL, err error) *url.URL {
	if err != nil {
		panic(err)
	}
	if u == nil {
		panic("url can't be nil")
	}
	return u
}

// String returns the string representation of the path with query string
func (o *CifsShareACLGetURL) String() string {
	return o.Must(o.Build()).String()
}

// BuildFull builds a full url with scheme, host, path and query string
func (o *CifsShareACLGetURL) BuildFull(scheme, host string) (*url.URL, error) {
	if scheme == "" {
		return nil, errors.New("scheme is required for a full url on CifsShareACLGetURL")
	}
	if host == "" {
		return nil, errors.New("host is required for a full url on CifsShareACLGetURL")
	}

	base, err := o.Build()
	if err != nil {
		return nil, err
	}

	base.Scheme = scheme
	base.Host = host
	return base, nil
}

// StringFull returns the string representation of a complete url
func (o *CifsShareACLGetURL) StringFull(scheme, host string) string {
	return o.Must(o.BuildFull(scheme, host)).String()
}
