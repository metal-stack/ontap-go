// Code generated by go-swagger; DO NOT EDIT.

package application

// This file was generated by the swagger tool.
// Editing this file might prove futile when you re-run the generate command

import (
	"errors"
	"net/url"
	golangswaggerpaths "path"
	"strings"

	"github.com/go-openapi/swag"
)

// ApplicationComponentSnapshotGetURL generates an URL for the application component snapshot get operation
type ApplicationComponentSnapshotGetURL struct {
	ApplicationUUID string
	ComponentUUID   string
	UUID            string

	Fields []string

	_basePath string
	// avoid unkeyed usage
	_ struct{}
}

// WithBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *ApplicationComponentSnapshotGetURL) WithBasePath(bp string) *ApplicationComponentSnapshotGetURL {
	o.SetBasePath(bp)
	return o
}

// SetBasePath sets the base path for this url builder, only required when it's different from the
// base path specified in the swagger spec.
// When the value of the base path is an empty string
func (o *ApplicationComponentSnapshotGetURL) SetBasePath(bp string) {
	o._basePath = bp
}

// Build a url path and query string
func (o *ApplicationComponentSnapshotGetURL) Build() (*url.URL, error) {
	var _result url.URL

	var _path = "/application/applications/{application.uuid}/components/{component.uuid}/snapshots/{uuid}"

	applicationUUID := o.ApplicationUUID
	if applicationUUID != "" {
		_path = strings.Replace(_path, "{application.uuid}", applicationUUID, -1)
	} else {
		return nil, errors.New("applicationUuid is required on ApplicationComponentSnapshotGetURL")
	}

	componentUUID := o.ComponentUUID
	if componentUUID != "" {
		_path = strings.Replace(_path, "{component.uuid}", componentUUID, -1)
	} else {
		return nil, errors.New("componentUuid is required on ApplicationComponentSnapshotGetURL")
	}

	uuid := o.UUID
	if uuid != "" {
		_path = strings.Replace(_path, "{uuid}", uuid, -1)
	} else {
		return nil, errors.New("uuid is required on ApplicationComponentSnapshotGetURL")
	}

	_basePath := o._basePath
	if _basePath == "" {
		_basePath = "/api"
	}
	_result.Path = golangswaggerpaths.Join(_basePath, _path)

	qs := make(url.Values)

	var fieldsIR []string
	for _, fieldsI := range o.Fields {
		fieldsIS := fieldsI
		if fieldsIS != "" {
			fieldsIR = append(fieldsIR, fieldsIS)
		}
	}

	fields := swag.JoinByFormat(fieldsIR, "csv")

	if len(fields) > 0 {
		qsv := fields[0]
		if qsv != "" {
			qs.Set("fields", qsv)
		}
	}

	_result.RawQuery = qs.Encode()

	return &_result, nil
}

// Must is a helper function to panic when the url builder returns an error
func (o *ApplicationComponentSnapshotGetURL) Must(u *url.URL, err error) *url.URL {
	if err != nil {
		panic(err)
	}
	if u == nil {
		panic("url can't be nil")
	}
	return u
}

// String returns the string representation of the path with query string
func (o *ApplicationComponentSnapshotGetURL) String() string {
	return o.Must(o.Build()).String()
}

// BuildFull builds a full url with scheme, host, path and query string
func (o *ApplicationComponentSnapshotGetURL) BuildFull(scheme, host string) (*url.URL, error) {
	if scheme == "" {
		return nil, errors.New("scheme is required for a full url on ApplicationComponentSnapshotGetURL")
	}
	if host == "" {
		return nil, errors.New("host is required for a full url on ApplicationComponentSnapshotGetURL")
	}

	base, err := o.Build()
	if err != nil {
		return nil, err
	}

	base.Scheme = scheme
	base.Host = host
	return base, nil
}

// StringFull returns the string representation of a complete url
func (o *ApplicationComponentSnapshotGetURL) StringFull(scheme, host string) string {
	return o.Must(o.BuildFull(scheme, host)).String()
}
